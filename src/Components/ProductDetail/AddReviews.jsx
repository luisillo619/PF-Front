import { useSelector } from "react-redux"

export const AddReviews = ()=> {

      const prod = useSelector(state => state.products2)
   
            return ( 

                <div>



                    
                </div>
    )
}